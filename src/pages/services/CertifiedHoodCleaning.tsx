import ServiceDetailPage from '../../components/ServiceDetailPage';
import { serviceDetails } from '../../data/siteContent';

export default function CertifiedHoodCleaningPage() {
  return <ServiceDetailPage service={serviceDetails.certifiedHoodCleaning} />;
}
